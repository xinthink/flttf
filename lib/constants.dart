import 'package:flutter/material.dart';

const double canvasSize = 200.0;
const double strokeWidth = 12.0;
const Color brushColor = Colors.black;
const bool isAntiAlias = true;
const int modelInputSize = 28;
const double canvasPadding = 40;
const double canvasScale = modelInputSize / (2 * canvasPadding + canvasSize);
